# 0022 - Persist per-book reading progress

- [x] Add `book_progress` table + migrations (`crates/storage`)
- [x] Add `Storage` APIs to get/set progress (`crates/storage`)
- [x] Load progress into app context on startup (`crates/app`, `crates/application`)
- [x] Run `cargo test --workspace`
