# 0020 - Settings menu + Scan Paths navigation

- [x] Add `s` Settings menu to open subpanels (`crates/ui`)
- [x] Remove `o`/`p` main shortcuts (`crates/ui`)
- [x] Make Scan Paths modal selection-driven with `↑/↓` and `←/→` for scan scope (`crates/ui`)
- [x] Run `cargo test --workspace`
