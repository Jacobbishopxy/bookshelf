# 0006 - Scan scope setting (direct vs recursive)

- [x] Add `ScanScope` and `scan_scope` to `Settings` (`crates/core`)
- [x] Migrate sqlite schema to include `scan_scope` and persist it (`crates/storage`)
- [x] Add UI entry to edit scan scope in settings panel (`crates/ui`)
- [x] Run `cargo test --workspace`
