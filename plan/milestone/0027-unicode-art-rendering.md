# 0027 - Unicode art rendering (non-AGPL)

- [x] Move `pdfium-render` into workspace deps (`Cargo.toml`, `crates/engine`)
- [x] Add Pdfium-backed raster render path (`crates/engine`)
- [x] Implement unicode art renderers for `Braille` and `Blocks` (`crates/engine`)
- [x] Use render mode in Reader (`crates/ui`)
- [x] Run `cargo test --workspace`
