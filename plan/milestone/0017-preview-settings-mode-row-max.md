# 0017 - Preview Settings: mode row + max values

- [x] Expose preview max constants (`crates/core`)
- [x] Render `text braille blocks` row with selected highlight (`crates/ui`)
- [x] Show max values for `Preview depth` and `Preview pages` (`crates/ui`)
- [x] Run `cargo test --workspace`
