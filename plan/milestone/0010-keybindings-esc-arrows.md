# 0010 - Keybindings: Esc quit, arrows move

- [x] Quit main UI with `Esc` instead of `q` (`crates/ui/src/lib.rs`)
- [x] Use `↑/↓` for navigation and remove `j/k` shortcuts (`crates/ui/src/lib.rs`)
- [x] Remove `q` quit shortcut from preview panel (`crates/ui/src/lib.rs`)
- [x] Run `cargo test --workspace`

