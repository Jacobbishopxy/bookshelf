# 0023 - Reader mode

- [x] Add engine helpers for page count + page text (`crates/engine`)
- [x] Add reader screen to UI (open with Enter, Esc back) (`crates/ui`)
- [x] Persist last page into `AppContext.progress_by_path` (`crates/ui`, `crates/application`)
- [x] Run `cargo test --workspace`
